# frozen_string_literal: true

# describe docker_container('phpmyadmin') do
#   it { should exist }
#   it { should be_running }
#   its('image') { should eq 'phpmyadmin/phpmyadmin:latest' }
#   its('repo') { should eq 'phpmyadmin/phpmyadmin' }
#   its('ports') { should eq '0.0.0.0:8888->80/tcp' }
# end
